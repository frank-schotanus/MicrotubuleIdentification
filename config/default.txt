# Example configuration file for training
# You can load this with: python train.py @config/default.txt

# Data paths
--mrc_dir=/path/to/mrc/files
--annotation_file=/path/to/annotations.txt
--output_dir=output/experiment1

# Model settings
--model_type=unet
--init_features=32
--target_type=heatmap
--heatmap_sigma=3.0

# Data preprocessing
--normalization=zscore
--augment

# Training hyperparameters
--batch_size=4
--num_workers=4
--lr=0.0001
--max_epochs=100
--patience=10

# Data split
--train_ratio=0.7
--val_ratio=0.15
--test_ratio=0.15
--random_seed=42

# Device
--device=auto
